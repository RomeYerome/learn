`HTML ENTITIES`;
// HTML entities are used to display reserved characters in HTML and special characters that aren't on standard keyboards like the copyright symbol. A reserved character is any character that has a special meaning in HTML. For example, the less than sign (<) is used to start an HTML tag. If you want to display the less than sign on a web page, you must use the HTML entity < which is `&lt;`

// HTML Entities start with an ampersand (&) and end with a semicolon (;) with the symbol code inbetween. The entity name is case sensitive. For example, &copy; is the copyright symbol, but &COPY; is not.

`List of HTML Entities`;

// Most Common Entities
`&lt;`  ;	    // less than
`&gt;`  ;	    // greater than
`&amp;`  ;	    // ampersand
`&quot;`  ;	    // double quotation mark
`&apos;`  ;	    // apostrophe
`&nbsp;`  ;	    // non-breaking space
`&copy;`  ;	    // copyright
`&reg;`  ;	    // registered trademark
`&trade;`  ;	// trademark
`&ne;`  ;	    // not equal
`&le;`  ;	    // less than or equal to
`&ge;`  ;	    // greater than or equal to
`&infin;`  ;	// infinity
`&pi;`  ;	    // pi
`&radic;`  ;	// square root
`&asymp;`  ;	// almost equal to
`&equiv;`  ;	// equivalent


// Other Entities
`&cent;`  ;	    // cent
`&pound;`  ;	// pound
`&yen;`  ;	    // yen
`&euro;`  ;	    // euro
`&times;`  ;	// multiplication
`&divide;`  ;	// division


`&cong;`  ;	    // congruent
`&sim;`  ;	    // similar
`&prop;`  ;	    // proportional to
`&part;`  ;	    // partial differential
`&nabla;`  ;	// nabla
`&int;`  ;	    // integral
`&sum;`  ;	    // summation
`&prod;`  ;	    // product
`&forall;`  ;	// for all
`&exist;`  ;	// there exists
`&empty;`  ;	// empty set
`&isin;`  ;	    // is in
`&notin;`  ;	// is not in
`&ni;`  ;	    // contains as member
`&and;`  ;	    // logical and
`&or;`  ;	    // logical or
`&not;`  ;	    // logical not
`&cap;`  ;	    // intersection
`&cup;`  ;	    // union
`&sub;`  ;	    // subset of
`&sup;`  ;	    // superset of
`&nsub;`  ;	    // not a subset of
`&sube;`  ;	    // subset of or equal to
`&supe;`  ;	    // superset of or equal to
`&oplus;`  ;	// circled plus
`&otimes;`  ;	// circled times
`&perp;`  ;	    // perpendicular
`&ang;`  ;	    // angle
`&deg;`  ;	    // degree
`&nabla;`  ;	// nabla
`&lowast;`  ;	// asterisk
`&loz;`  ;	    // lozenge
`&spades;`  ;	// spade suit
`&clubs;`  ;	// club suit
`&hearts;`  ;	// heart suit
`&diams;`  ;	// diamond suit
`&quot;`  ;	    // quotation mark



