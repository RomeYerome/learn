`HTML`;
// stands for HyperText Markup Language. It is the standard markup language used to create webpages. HTML is used to structure content on web pages. It gives definition/context to the content on a webpage using tags (elements) like headings, paragraphs, images, links, etc.

// HTML is not a programming language. It is a markup language. It is used to create the structure of a webpage.

// HTML is made up of elements. An element is a piece of content in a webpage. An element is made up of a start tag, content, and an end tag. The start tag is the opening tag and the end tag is the closing tag. The content is the text or image or link that is inside the tags.


`TAGS`;
// Tags are used to create elements and define the structure of a webpage. They are enclosed in angle brackets. There are 2 types of tags: 

`self closing tags`; // which are tags that don't have any content e.g <br> 
`container tags`; //  e.g <p>some text</p>.

